# Este archivo se dividió en requirements/{base,dev,prod}.txt
# Instalación:
#   desarrollo → pip install -r requirements/dev.txt
#   producción → pip install -r requirements/prod.txt
-r requirements/prod.txt
